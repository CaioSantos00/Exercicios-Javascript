/*const naves = [
    ["Fenix", 8, true],
    ["Golias", 10, true],
    ["Helmet", 5, false],
    ["Elemental", 3, true],
    ["Darwin", 15, false],
]

let filtro = naves.filter((nome) => {
    return nome[1] >= 9
})
let situacao = naves.findIndex((estado) => {
    return estado[2] == false
})
let maiusculo = naves.map((letram) => {
    return letram[0].toUpperCase()
})

let mensagem = "Naves com 9 tripulantes: " + filtro.join(", ")
mensagem += "\n Plataformas com engate: " + situacao
mensagem += "\n Destacadas: " + maiusculo.join(", ")

alert(mensagem)*/
